<% conversations.each do |conversation| %>
  <% user = chat_user(conversation) %>
  <%= render 'conversations/conversation', conversation: conversation, user: user %>
<% end %>
<%= link_to 'See All', conversations_path, class: 'dropdown-item text-right float-right conversation-item pr-3'%>