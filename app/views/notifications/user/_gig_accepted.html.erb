<div class="card">
  <div class="card-body">
    <h5><%= actor.name %> accepted your gig</h5>
    <em class="text-muted"><%= notification.created_at.strftime('%e %b %Y %H:%M %p') %></em>
  </div>
</div>