<div class="card mb-1">
  <div class="card-body">
    <h5>
      Invitation to join the group <%= link_to notification.notifiable&.group&.name, group_path(notification.notifiable.group) %> has been rejected.
    </h5>
    <em class="text-muted"><%= notification.created_at.strftime('%e %b %Y %H:%M %p') %></em>
  </div>
</div>
