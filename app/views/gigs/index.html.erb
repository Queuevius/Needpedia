<%= render 'shared/offer_buttons' %>

<div class="container mb-5">
  <div class="row mt-3">
    <div class="col-md-12 text-center mb-2">
      <h1><b>My Gigs</b></h1>
    </div>
    <div class="col-md-12">
    <div class="row">
      <div class="col-sm-6">
      </div>

      <div class="col-sm-6 text-right mb-2">
        <%= link_to new_gig_path(), class: 'btn btn-primary' do %>
          Add New
        <% end %>
      </div>
    </div>

    <%= render 'list', gigs: @gigs %>
    </div>
  </div>
</div>
