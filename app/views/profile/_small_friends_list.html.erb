<% friends.each do |friend| %>
  <div class="col-lg-3 col-md-6 wrap-feature14-box p-t-20 p-l-20 p-r-20 friends-profiles">
    <div class="aos-init">
      <% if friend.profile_image.attached? %>
        <%= image_tag friend.profile_image.variant(resize: "250x250"), class: 'img-responsive' %>
      <% else %>
        <%= image_tag "profile.png", class: 'img-responsive', style: 'width: 160px; height: 160px' %>
      <% end %>
      <div class="mb-5">
        <%= render 'friend_card_details', friend: friend %>
      </div>
    </div>
  </div>
<% end %>