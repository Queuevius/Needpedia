<%= form_with(model: [@post, Objective.new], local: true) do |form| %>

  <%= form.hidden_field :user_id, value: current_user.id %>
  <%= form.hidden_field :post_id, value: @post.id %>

  <div class="form-group">
    <%= form.text_area :content, autofocus: true, rows: 20, class: 'form-control', placeholder: "Body", id: 'objective-summernote' %>
  </div>
  <div class="form-group ml-2">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
<script>
    $('#objective-summernote').summernote({
        height: 150,
        disableDragAndDrop: true,
        toolbar: [
// [groupName, [list of button]]
            ['style', ['bold', 'italic', 'underline', 'link']],
            ['font', ['strikethrough', 'fontsize']],
            ['para', ['ul', 'ol', 'paragraph']],
            ['tokens', ['note', 'question', 'debate']],
            ['misc', ['undo', 'redo']]
        ],
        popover: {
            image: [
                ['remove', ['removeMedia']]
            ],
            link: []
        },

    });
</script>
